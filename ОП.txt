#include <iostream>
using namespace std;

int main() {
    int number;
    cout << "Input number ";
    cin >> number;

    (number % 2 != 0 && number >= 100 && number <= 999) ? cout << "true" : cout << "false";

    return 0
}
