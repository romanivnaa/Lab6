#include <iostream>
#include <math.h>
#include<iomanip>

using namespace std;

int main() {
    float x, sum, y;
    cout << "Input x: ";
    cin >> x;
    sum = 0;
    for (int i = 1; i <= 5; i++) {
        sum += cos(i);
    }
    y = (pow(tan(x), 2) + sum) / x + 5;
    cout << "Y = " << y;
}
    sum = 0;
    sum = 0;
    sum = 0;
    sum = 0;
